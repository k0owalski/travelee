import styled from 'styled-components';

const AirportSelect = styled.div`
  display: flex;
  justify-content: center;
  align-items: center;

  width: 56%;

  margin: 1rem 0 0;
  padding: 0.5rem 0;

  font-size: 1.5rem;

  background-color: rgba(255, 255, 255, 0.42);
  border-radius: 0.5rem;

  cursor: pointer;
`;

export default AirportSelect;
